###############################################################################
#                          Generated on phpdocker.io                          #
###############################################################################
version: "3.1"
services:

    postgres:
      image: postgres:11.1-alpine
      container_name: curso-repository-postgres
      working_dir: /application
      volumes:
        - .:/application
      environment:
        - POSTGRES_USER=ismael
        - POSTGRES_PASSWORD=202011aa
        - POSTGRES_DB=curso-repository
      ports:
        - "5432:5432"

    webserver:
      image: nginx:alpine
      container_name: curso-repository-webserver
      working_dir: /application
      volumes:
          - .:/application
          - ./phpdocker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      ports:
       - "8081:80"

    php-fpm:
      build: phpdocker/php-fpm
      container_name: curso-repository-php-fpm
      working_dir: /application
      volumes:
        - .:/application
        - ./phpdocker/php-fpm/php-ini-overrides.ini:/etc/php/7.4/fpm/conf.d/99-overrides.ini

